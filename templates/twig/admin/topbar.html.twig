<nav id="adminNav" class="navbar navbar-expand-lg">
    <div class="container">
        <div class="adminMenu">
            <ul class="list-inline text-left">
                <li class="list-inline-item itemMenu">
                    <a href="index.php"><i class="fas fa-home"></i>Accueil</a>
                </li>
                {% if session.get('pseudo') and session.get('role') == 'admin' %}
                    <li class="list-inline-item">
                        <a href="index.php?route=addArticle"><i class="far fa-plus-square"></i>Ajouter un post</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div class="userMenu dropdown">
            <button class="dropdown-toggle" type="button" data-toggle="dropdown">
                <i class="fas fa-user"></i> {{ session.get('pseudo') }}
            </button>
            <div class="dropdown-menu">
                <a class="dropdown-item" href="index.php?route=profile"><span class="far fa-address-card"></span> Mon profil</a>
                {% if session.get('pseudo') and session.get('role') == 'admin' %}
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="index.php?route=administration"><span class="fas fa-user-cog"></span> Administration</a>
                {% endif %}
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="index.php?route=logout"><span class="fas fa-sign-out-alt"></span> Se déconnecter</a>
            </div>
        </div>
    </div>
</nav>